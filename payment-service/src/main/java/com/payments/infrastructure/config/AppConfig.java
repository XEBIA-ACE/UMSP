package com.payments.infrastructure.config;

import org.springframework.context.annotation.Configuration;

/**
 * Central Spring application configuration class.
 *
 * <p>This class is intentionally minimal. Add {@code @Bean} definitions here as
 * the application grows — for example:
 * <ul>
 *   <li>Jackson {@code ObjectMapper} customisation</li>
 *   <li>Clock bean for testable time handling</li>
 *   <li>Async executor configuration</li>
 *   <li>Cache manager setup</li>
 * </ul>
 *
 * <p>TODO: Add application-level beans as requirements evolve.
 */
@Configuration
public class AppConfig {
    // TODO: Add @Bean definitions as the application grows.
}
