# Web framework — Flask 3.x (upgraded from 1.x, EOL).
# Werkzeug 3.x is required by Flask 3.x and replaces the 0.x/1.x line used with Flask 1.x.
Flask==3.1.0
Werkzeug==3.1.3
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.1.8
blinker==1.9.0
MarkupSafe==3.0.2

# Test / lint tooling
pytest>=8.0
flake8>=7.0
